<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="com.tfjybj.english.provider.dao.UserWrongDao">
	<!--根据UserWrong用户id模糊查询UserWrongEntity-->
    <select id="queryLikeUserId" resultType="com.tfjybj.english.entity.UserWrongEntity">
        SELECT
			*
        FROM
			tn_user_wrong f
        WHERE
			f.is_delete = 0
        <if test="userId !=''">
            <bind name="pattern" value="'%' + userId + '%'"/>
            AND f.user_id LIKE #{pattern}
        </if>
        ORDER BY
        f.create_time DESC
    </select>
    <!--根据UserWrong用户id查询错误单词-->
    <select id="queryWrongWordId" resultType="com.tfjybj.english.model.UserWrongModel">
        SELECT
            *
        FROM
            tn_user_wrong
        WHERE
            user_id = #{userId}
            AND type = 1
            AND is_delete =0
    </select>

    <!--根据UserWrong用户id查询错误音标-->
    <select id="queryWrongPhoneficId" resultType="com.tfjybj.english.model.UserWrongModel">
        SELECT
            *
        FROM
            tn_user_wrong
        WHERE
            user_id = #{userId}
            AND type = 2
            AND is_delete =0
    </select>

</mapper>